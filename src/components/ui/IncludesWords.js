import React from 'react';
import { CardTravel } from '@mui/icons-material';
const IncludesWords = ({ words }) => {
  return (
    <button className="btn btn-dark">
      <CardTravel />
    </button>
  );
};

export default IncludesWords;
